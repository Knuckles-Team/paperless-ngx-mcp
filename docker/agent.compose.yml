version: '3.8'

services:
  paperless-ngx-mcp-mcp:
    image: ${PAPERLESS_NGX_MCP_IMAGE:?set-PAPERLESS_NGX_MCP_IMAGE-to-image@sha256-digest}
    container_name: paperless-ngx-mcp-mcp
    hostname: paperless-ngx-mcp-mcp
    restart: always
    env_file:
      - ../.env
    environment:
      - PYTHONUNBUFFERED=1
      - HOST=0.0.0.0
      - PORT=8000
      - TRANSPORT=streamable-http
      - ENABLE_OTEL=False
      - LANGFUSE_CAPTURE_CONTENT=False
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  paperless-ngx-mcp-agent:
    image: ${PAPERLESS_NGX_AGENT_IMAGE:?set-PAPERLESS_NGX_AGENT_IMAGE-to-image@sha256-digest}
    container_name: paperless-ngx-mcp-agent
    hostname: paperless-ngx-mcp-agent
    restart: always
    depends_on:
      - paperless-ngx-mcp-mcp
    env_file:
      - ../.env
    command: [ "paperless-ngx-agent" ]
    environment:
      - PYTHONUNBUFFERED=1
      - HOST=0.0.0.0
      - PORT=9000
      - MCP_URL=http://paperless-ngx-mcp-mcp:8000/mcp
      - PROVIDER=${PROVIDER:-openai}
      - MODEL_ID=${MODEL_ID:-gpt-4o}
      - ENABLE_WEB_UI=True
      - ENABLE_OTEL=False
      - LANGFUSE_CAPTURE_CONTENT=False
    ports:
      - "9000:9000"
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
